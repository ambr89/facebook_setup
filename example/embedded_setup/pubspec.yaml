name: embedded_setup
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  facebook_setup: "^0.0.4"

facebook_setup:
  fb_app_id: "YOUR_FB_APP_ID"
  fb_app_name: "My App Name"
  android: true
  ios: true